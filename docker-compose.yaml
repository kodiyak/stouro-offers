version: "3.8"

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: aurauser
      POSTGRES_PASSWORD: aurapass
      POSTGRES_DB: auradb
    ports:
      - "9032:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
